services:
  rapidocr:
    build: ./rapidocr_service
    ports:
      - "8001:8001"

  app:
    build: .
    environment:
      BOT_TOKEN: "${BOT_TOKEN}"
      RAPIDOCR_URL: "http://rapidocr:8001"
      PORT: "${PORT:-8000}"
    depends_on:
      - rapidocr
